function toggleDarkMode(){
  document.body.classList.toggle("dark");
}
function countChart(){
  let tex=document.getElementByid("message").value;
  document.getElementByid("message").innerText=text.length;
}
